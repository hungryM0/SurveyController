interface:
  display_name: "Survey HTTP Core"
  short_description: "SurveyCore submodule HTTP core"
  default_prompt: "Use $survey-http-core for SurveyController's SurveyCore submodule, proxycore, surveycore, platform adapters, or public core contract changes."
